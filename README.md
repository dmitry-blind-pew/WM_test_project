# Отчет о потреблении кофе

Скрипт читает один или несколько CSV-файлов с данными о подготовке студентов и выводит в консоль таблицу с медианной суммой трат на кофе по каждому студенту.  
Поддерживаемый отчет: `median-coffee`.

## Пример запуска

```bash
python main.py --files data/math.csv data/physics.csv data/programming.csv --report median-coffee
```

## Что важно для ревью

- в функции [median_coffee_report](src/reports.py#L22) добавлены заголовки для рендеринга, а не в функции [render_report](src/utils.py#L5), для абстракции рендеринга, и возможности масштабирования при добавлении новых типов отчетов.
- в [аннотации](src/reports.py#L43) оставил синтаксис "...", потому что счел более важным оставить во всех функциях именованные параметры, мне кажется глобально это лучше.(могу ошибаться).
- обработку PermissionError и csv.Error не делал, исходя из условия задания.
- при чтении данных о coffee_spent преобразую в [int](src/src/reports.py#L34), чтобы в будущем не добавлять еще округление при работе с float, т.к. задание тестовое, но понимаю что на проде стоит сделать иначе.